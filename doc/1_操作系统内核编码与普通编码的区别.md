# 内核编码与运行与普通编码的区别
## 编码区别
### 无常见定义的main入口
在日常编码过程中，基本有一个main函数作为程序的入口，但是内核的入口函数是固定的，是内核的入口。
需要在内核的入口汇编函数中，需要在通过汇编调起C语言入口函数（签名不一定是main）。
并且代码需要通过一定方式链接，将他们的二进制文件物理连接在一起，调其main函数的时候直接将pc跳转到内核的入口函数。

## 编译区别
